% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rater_analysis.R
\name{plot_rater_map}
\alias{plot_rater_map}
\title{Plot raters in the bias-magnifier plane}
\usage{
plot_rater_map(
  x,
  probs = c(0.025, 0.975),
  show_intervals = FALSE,
  group = FALSE
)
}
\arguments{
\item{x}{A fitted object of class \code{"BayesODT"}.}

\item{probs}{Length-2 numeric vector of posterior probabilities used for
credible intervals.}

\item{show_intervals}{Logical; if \code{TRUE}, draw horizontal and vertical
credible intervals for \eqn{a_j} and \eqn{b_j}.}

\item{group}{Logical; if \code{TRUE}, color points by rater group.}

\item{show_labels}{Logical; if \code{TRUE}, label each point by rater index.}
}
\value{
Invisibly returns a data frame of posterior summaries used in the plot.
}
\description{
Plot posterior summaries of rater-specific bias \eqn{a_j} and magnifier
\eqn{b_j} in a two-dimensional map. Each point corresponds to one rater.
}
\details{
The \eqn{a_j} values are row-wise centered so that the plot represents
relative rater bias.

If \code{group = TRUE}, raters are colored according to the group assignments
returned by \code{group_raters()}.
}
