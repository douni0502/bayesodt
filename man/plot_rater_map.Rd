% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rater_analysis.R
\name{plot_rater_map}
\alias{plot_rater_map}
\title{Plot raters in the bias-magnifier plane}
\usage{
plot_rater_map(
  x,
  probs = c(0.025, 0.975),
  show_labels = TRUE,
  show_intervals = FALSE,
  color_by = c("none", "group", "cluster"),
  a_cut = NULL,
  b_cut = NULL,
  group_labels = c("low bias / low magnifier", "high bias / low magnifier",
    "low bias / high magnifier", "high bias / high magnifier"),
  group_cols = c("gray40", "tomato", "dodgerblue3", "darkgreen"),
  cluster_k = NULL,
  cluster_k_max = 6L,
  cluster_scale = TRUE,
  cluster_cols = NULL,
  legend_pos = "topright",
  xlab = expression(a[j] ~ "(bias)"),
  ylab = expression(b[j] ~ "(magnifier)"),
  main = "Scatter plot of Rater",
  pch = 19,
  cex = 1,
  ...
)
}
\arguments{
\item{x}{A fitted object of class \code{"BayesODT"}.}

\item{probs}{Length-2 numeric vector of posterior probabilities used for
credible intervals.}

\item{show_labels}{Logical; if \code{TRUE}, label each point by rater index.}

\item{show_intervals}{Logical; if \code{TRUE}, draw horizontal and vertical
credible intervals for \eqn{a_j} and \eqn{b_j}.}

\item{color_by}{Coloring rule. One of \code{"none"}, \code{"group"},
or \code{"cluster"}.}

\item{a_cut}{Cutoff for bias grouping. Used only when
\code{color_by = "group"}. If \code{NULL}, uses 0.}

\item{b_cut}{Cutoff for magnifier grouping. Used only when
\code{color_by = "group"}. If \code{NULL}, uses the median of posterior
mean magnifiers.}

\item{group_labels}{Character vector of length 4 giving group labels in the
order used by \code{group_raters()}.}

\item{group_cols}{Character vector of length 4 specifying colors for groups.}

\item{cluster_k}{Number of clusters used when \code{color_by = "cluster"}.
If \code{NULL}, the number of clusters is chosen automatically.}

\item{cluster_k_max}{Maximum number of clusters considered when
\code{cluster_k = NULL}.}

\item{cluster_scale}{Logical; if \code{TRUE}, standardize variables before clustering.}

\item{cluster_cols}{Optional vector of colors for clusters. If \code{NULL},
colors are generated automatically.}

\item{legend_pos}{Position for the legend.}

\item{xlab}{X-axis label.}

\item{ylab}{Y-axis label.}

\item{main}{Plot title.}

\item{pch}{Plotting symbol.}

\item{cex}{Point size.}

\item{...}{Further graphical arguments passed to \code{plot()}.}
}
\value{
Invisibly returns a data frame of posterior summaries used in the plot.
}
\description{
Plot posterior summaries of rater-specific bias \eqn{a_j} and magnifier
\eqn{b_j} in a two-dimensional map. Each point corresponds to one rater.
}
\details{
The \eqn{a_j} values are row-wise centered, following the convention used
in \code{plot_ABU()}.
}
