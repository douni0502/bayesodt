% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{gelman_diag}
\alias{gelman_diag}
\title{Gelman-Rubin diagnostic for BayesODT posterior samples}
\usage{
gelman_diag(
  x,
  param = c("A", "a", "tau_a", "b", "mu_b", "tau_b", "u"),
  index = 1L,
  start = NULL,
  thin = NULL,
  autoburnin = FALSE,
  multivariate = FALSE,
  transform = FALSE,
  confidence = 0.95,
  ...
)
}
\arguments{
\item{x}{A fitted object of class \code{"BayesODT"}.}

\item{param}{Character string specifying which parameter to evaluate.
One of \code{"A"}, \code{"a"}, \code{"tau_a"}, \code{"b"}, \code{"mu_b"}, \code{"tau_b"}, or \code{"u"}.}

\item{index}{Integer index (or vector of indices) of the selected component(s).
For scalar hyperparameters such as \code{"tau_a"}, \code{"mu_b"}, and \code{"tau_b"},
only \code{index = 1} is valid.}

\item{start}{Optional starting iteration for the \code{coda::mcmc} object.
If \code{NULL}, uses 1.}

\item{thin}{Optional thinning interval for the \code{coda::mcmc} object.
If \code{NULL}, uses \code{x$mcmc_info$n.thin} when available, otherwise 1.}

\item{autoburnin}{Logical; passed to \code{coda::gelman.diag()}.}

\item{multivariate}{Logical; passed to \code{coda::gelman.diag()}.}

\item{transform}{Logical; passed to \code{coda::gelman.diag()}.}

\item{confidence}{Confidence level for the upper confidence limit.}

\item{...}{Not used.}
}
\value{
A data frame with columns:
\describe{
\item{parameter}{Parameter name.}
\item{psrf}{Point estimate of the potential scale reduction factor.}
\item{upper_ci}{Upper confidence limit of the PSRF.}
}
}
\description{
Compute Gelman-Rubin convergence diagnostics for selected posterior samples
from a \code{"BayesODT"} object using \pkg{coda}.
}
