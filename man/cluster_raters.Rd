% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rater_analysis.R
\name{cluster_raters}
\alias{cluster_raters}
\title{Cluster raters based on posterior summaries of bias and magnifier}
\usage{
cluster_raters(
  x,
  k = NULL,
  k_max = 6L,
  scale = TRUE,
  hclust_method = "complete",
  probs = c(0.025, 0.975),
  ...
)
}
\arguments{
\item{x}{A fitted object of class \code{"BayesODT"}.}

\item{k}{Number of clusters. If \code{NULL}, choose automatically.}

\item{k_max}{Maximum number of clusters considered when \code{k = NULL}.}

\item{scale}{Logical; if \code{TRUE}, standardize \eqn{a_j} and \eqn{b_j}
before clustering.}

\item{hclust_method}{Linkage method passed to \code{stats::hclust()}.}

\item{probs}{Length-2 numeric vector of posterior probabilities used for
credible intervals in the returned summary.}

\item{...}{Not used.}
}
\value{
A data frame with cluster assignments and posterior summaries.
The chosen clustering object is stored in \code{attr(, "fit")}. If
\code{k = NULL}, the selected number of clusters is stored in
\code{attr(, "k_selected")} and the silhouette scores are stored in
\code{attr(, "silhouette_scores")}.
}
\description{
Cluster raters using hierarchical clustering on posterior means of centered
\eqn{a_j} and \eqn{b_j}. If \code{k} is not supplied, the number of clusters
is chosen automatically by maximizing the average silhouette width.
}
