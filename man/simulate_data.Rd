% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_generation.R
\name{simulate_data}
\alias{simulate_data}
\title{Simulate multirater ordinal diagnostic test data}
\usage{
simulate_data(
  m = 50,
  n = 10,
  K = 4,
  alpha = c(-1, 1, 2),
  mu_a = 0,
  var_a = 0.5,
  mu_b = 1.7,
  var_b = 0.5,
  u_mean1 = -1.5,
  u_sd1 = 0.5,
  u_mean2 = 1.5,
  u_sd2 = 0.5,
  u_weight = 0.9
)
}
\arguments{
\item{m}{Integer. Number of items (subjects).}

\item{n}{Integer. Number of raters.}

\item{K}{Integer. Number of ordinal categories.}

\item{alpha}{Numeric vector of length \code{K - 1}. Ordered cutpoints for the
ordinal rating model.}

\item{mu_a}{Numeric. Mean of the rater bias parameters \eqn{a_j}.}

\item{var_a}{Numeric. Variance of the rater bias parameters \eqn{a_j}.}

\item{mu_b}{Numeric. Mean of the rater magnifier parameters \eqn{b_j}.}

\item{var_b}{Numeric. Variance of the rater magnifier parameters \eqn{b_j}.}

\item{u_mean1}{Numeric. Mean of the first mixture component for the latent
disease severity \eqn{u_i}.}

\item{u_sd1}{Numeric. Standard deviation of the first mixture component for
\eqn{u_i}.}

\item{u_mean2}{Numeric. Mean of the second mixture component for the latent
disease severity \eqn{u_i}.}

\item{u_sd2}{Numeric. Standard deviation of the second mixture component for
\eqn{u_i}.}

\item{u_weight}{Numeric between 0 and 1. Mixing weight for the first mixture
component of \eqn{u_i}.}
}
\value{
A list with the following components:
\describe{
\item{W}{An \eqn{m \times n} matrix of ordinal ratings.}
\item{D}{A binary vector of length \eqn{m} containing the true disease status.}
\item{true_AUC}{The true AUC computed from the generated data.}
}
}
\description{
Generate one simulated dataset from a multirater ordinal diagnostic model.
The function returns an ordinal rating matrix, the true binary disease status,
and the true AUC computed from the generated data.
}
\details{
The latent disease severity \eqn{u_i} is generated from a two-component
normal mixture distribution. The true disease status \eqn{D_i} is then
sampled from a Bernoulli distribution with success probability
\eqn{\Phi(u_i)}, where \eqn{\Phi} is the standard normal cumulative
distribution function.

For each rater \eqn{j} and item \eqn{i}, the ordinal rating \eqn{W_{ij}} is
generated from category probabilities
\deqn{
P(W_{ij} = k) =
\Phi(\alpha_k - (a_j + b_j u_i)) -
\Phi(\alpha_{k-1} - (a_j + b_j u_i)),
}
for \eqn{k = 1, \dots, K}.

The returned \code{true_AUC} is computed from the generated diseased and
nondiseased rating distributions.
}
\examples{
sim <- simulate_data()
str(sim$W)
table(sim$D)
sim$true_AUC

}
