% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{plot_density}
\alias{plot_density}
\title{Density plot for BayesODT posterior samples}
\usage{
plot_density(
  x,
  param = c("A", "a", "tau_a", "b", "mu_b", "tau_b", "u"),
  index = 1L,
  start = NULL,
  thin = NULL,
  ask = FALSE,
  ...
)
}
\arguments{
\item{x}{A fitted object of class \code{"BayesODT"}.}

\item{param}{Character string specifying which parameter to plot.
One of \code{"A"}, \code{"a"}, \code{"tau_a"}, \code{"b"}, \code{"mu_b"}, \code{"tau_b"}, or \code{"u"}.}

\item{index}{Integer index (or vector of indices) of the selected component(s).
For scalar hyperparameters such as \code{"tau_a"}, \code{"mu_b"}, and \code{"tau_b"},
only \code{index = 1} is valid.}

\item{start}{Optional starting iteration for the \code{coda::mcmc} object.
If \code{NULL}, uses 1.}

\item{thin}{Optional thinning interval for the \code{coda::mcmc} object.
If \code{NULL}, uses \code{x$mcmc_info$n.thin} when available, otherwise 1.}

\item{ask}{Logical; if \code{TRUE}, ask before moving to a new figure.}

\item{...}{Further graphical arguments passed to \code{coda::densplot()}.}
}
\value{
Invisibly returns the created \code{"mcmc"} or \code{"mcmc.list"} object.
}
\description{
Plot posterior density estimates for selected posterior samples from a
\code{"BayesODT"} object using \pkg{coda}.
}
