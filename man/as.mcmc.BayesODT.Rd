% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{as.mcmc.BayesODT}
\alias{as.mcmc.BayesODT}
\title{Coerce a BayesODT object to coda mcmc objects
Convert selected posterior samples from a \code{"BayesODT"} object to a \code{coda::mcmc} object or a \code{coda::mcmc.list} object.}
\usage{
\method{as.mcmc}{BayesODT}(
  x,
  param = c("A", "a", "tau_a", "b", "mu_b", "tau_b", "u"),
  index = 1L,
  start = NULL,
  thin = NULL
)
}
\arguments{
\item{x}{A fitted object of class \code{"BayesODT"}.}

\item{param}{Character string specifying which parameter to extract. One of \code{"A"}, \code{"a"}, \code{"tau_a"}, \code{"b"}, \code{"mu_b"}, \code{"tau_b"}, or \code{"u"}.}

\item{index}{Integer index (or vector of indices) of the selected component(s).
If \code{index = NULL}, all components of the selected parameter are returned.
For scalar hyperparameters such as \code{"tau_a"}, \code{"mu_b"}, and \code{"tau_b"}, only \code{index = 1} or \code{index = NULL} is valid.}

\item{start}{Optional starting iteration for the \code{coda::mcmc} object. If \code{NULL}, uses 1.}

\item{thin}{Optional thinning interval for the \code{coda::mcmc} object. If \code{NULL}, uses \code{x$mcmc_info$n.thin} when available, otherwise 1.}
}
\value{
If \code{x} contains one chain, returns an object of class \code{"mcmc"}.
If \code{x} contains multiple chains, returns an object of class \code{"mcmc.list"}.
}
\description{
Coerce a BayesODT object to coda mcmc objects
Convert selected posterior samples from a \code{"BayesODT"} object to a \code{coda::mcmc} object or a \code{coda::mcmc.list} object.
}
