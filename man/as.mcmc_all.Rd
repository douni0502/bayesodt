% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{as.mcmc_all}
\alias{as.mcmc_all}
\title{Coerce all selected BayesODT posterior samples to coda mcmc objects}
\usage{
as.mcmc_all(
  x,
  params = c("A", "a", "tau_a", "b", "mu_b", "tau_b", "u"),
  start = NULL,
  thin = NULL
)
}
\arguments{
\item{x}{A fitted object of class \code{"BayesODT"}.}

\item{params}{Character vector specifying which parameters to extract.
Any subset of \code{"A"}, \code{"a"}, \code{"tau_a"}, \code{"b"}, \code{"mu_b"}, \code{"tau_b"}, and \code{"u"}.
By default, all are included.}

\item{start}{Optional starting iteration for the \code{coda::mcmc} object.
If \code{NULL}, uses 1.}

\item{thin}{Optional thinning interval for the \code{coda::mcmc} object.
If \code{NULL}, uses \code{x$mcmc_info$n.thin} when available, otherwise 1.}
}
\value{
If \code{x} contains one chain, returns an object of class \code{"mcmc"}.
If \code{x} contains multiple chains, returns an object of class \code{"mcmc.list"}.
}
\description{
Convert multiple posterior sample blocks from a \code{"BayesODT"} object to a
single \code{coda::mcmc} object or a \code{coda::mcmc.list} object by column-binding
all selected parameters within each chain.
}
